class Solution {
private: 
    int sum(vector<int> nums) {
        int sum = 0;
        for (int n : nums) sum += n;
        return sum;
    }

public:
    bool canPartition(vector<int>& nums) {
        int totalSum = 
        return false;
    }
};
